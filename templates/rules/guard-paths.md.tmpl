# Guard paths — opus-on-touch convention

These paths ARE this project's own review/governance mechanism — the hooks
that gate every commit, the reviewer agents, the routing that decides which
reviewer runs, and the CI that backstops all of it. A defect here doesn't
just ship a bug; it can silently weaken or disable the thing that's supposed
to catch defects in the first place.

**Convention**: when a diff you're about to send for review touches any path
in the list below, spawn the required reviewer(s) — <REPLACE: the
guard-path-routed reviewer name(s) for this project, e.g. "platform-reviewer">
— with `model: opus` instead of their frontmatter default. <REPLACE: any
always-required reviewer that should stay exempt, e.g. "scope-auditor">
is exempted: it's required on every commit regardless of path, so escalating
it here would multiply cost for the highest-frequency reviewer for no
proportionate benefit.

**This is a procedural convention, not hook-enforced.** A `PreToolUse(Bash)`
hook cannot see a Task-tool subagent spawn, so there is no way to mechanically
verify which model a reviewer actually ran on. Follow it because it's the
right call on a high-stakes diff, not because anything will stop you if you
don't. A parity test (see `.claude/tests/test_routing_doc_parity.py` in
`claude-project-kit` for the pattern) can only guarantee this list and
`review_routing.json` agree with each other — it can't verify a reviewer was
actually spawned at the promoted model.

## Guard paths

<!-- REPLACE with this project's actual guard paths — the routing entries
     pointing at whichever reviewer(s) own platform/CI/hooks/governance
     concerns. Keep this list and review_routing.json's routing in sync; a
     parity test modeled on test_routing_doc_parity.py is the way to catch
     drift automatically rather than trusting memory. -->
- <path pattern 1>
- <path pattern 2>

## Exempted from escalation

- <always-required reviewer name(s)>
